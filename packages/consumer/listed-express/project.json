{
  "name": "listed-express",
  "projectType": "library",
  "tags": ["platform:node", "scope:consumer"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/consumer/listed-express/tsconfig.json" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/consumer/listed-express/tsconfig.json --noEmit" } },
    "lint": { "executor": "@nx/eslint:lint" }
  }
}
