{
  "name": "listed-react",
  "projectType": "library",
  "tags": ["platform:browser", "scope:consumer"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/consumer/listed-react/tsconfig.json" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/consumer/listed-react/tsconfig.json --noEmit" } },
    "lint": { "executor": "@nx/eslint:lint" }
  }
}
