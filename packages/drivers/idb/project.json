{
  "name": "listed-idb",
  "projectType": "library",
  "tags": ["platform:browser", "scope:driver"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/drivers/idb/tsconfig.json" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/drivers/idb/tsconfig.json --noEmit" } },
    "lint": { "executor": "@nx/eslint:lint" }
  }
}
