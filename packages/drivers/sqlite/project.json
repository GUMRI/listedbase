{
  "name": "listed-sqlite",
  "projectType": "library",
  "tags": ["platform:node", "scope:driver"],
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/drivers/sqlite/tsconfig.json" } },
    "typecheck": { "executor": "nx:run-commands", "options": { "command": "tsc -p packages/drivers/sqlite/tsconfig.json --noEmit" } },
    "lint": { "executor": "@nx/eslint:lint" }
  }
}
