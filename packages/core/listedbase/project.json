{
  "name": "listedbase",
  "projectType": "library",
  "tags": ["platform:isomorphic", "scope:core"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p packages/core/listedbase/tsconfig.json" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p packages/core/listedbase/tsconfig.json --noEmit" }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
